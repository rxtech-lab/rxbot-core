import { RouteMetadata } from "@rx-lab/common";
import { CommandButton } from "@rx-lab/core";

export const metadata: RouteMetadata = {
  title: "Hello world",
  description: "This is a simple welcome page",
  includeInMenu: true,
};

export default function page() {
  return (
    <div>
        <h1>Welcome to Rx Bot</h1>
        <p>This is a simple welcome page</p>
        <CommandButton command="/help" text="Help" />
    </div>
  );
}
